# =============================================================
# Projet RAG - LO17/AI31 (UTC, Printemps 2026)
# Corpus : RGPD (texte juridique francais)
# Pile : 100% local (embeddings + LLM HuggingFace, pas de Gemini)
# Python recommande : 3.10 ou 3.11
# =============================================================

# --- Ecosysteme LangChain (packages eclates depuis la v0.2) ---
langchain-core
langchain
langchain-community
langchain-text-splitters

# --- Base de donnees vectorielle ---
chromadb

# --- Embeddings, reranker et LLM local (HuggingFace) ---
sentence-transformers      # HuggingFaceEmbeddings + CrossEncoder (reranker)
transformers               # pipeline + Qwen2.5-0.5B-Instruct
torch                      # backend des modeles (device="cpu" dans ton code)

# --- Recuperation et parsing du corpus RGPD (HTML) ---
requests
beautifulsoup4
lxml                       # parser rapide pour BeautifulSoup

# --- Securite des cles / variables d'environnement ---
python-dotenv

# --- Interface (livrable demande par les consignes) ---
streamlit

# --- Notebook uniquement (affichage HTML des reponses) ---
# Deja inclus avec Jupyter ; a retirer pour l'app Streamlit
ipython