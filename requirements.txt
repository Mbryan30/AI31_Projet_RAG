# =============================================================================
#  AI31_Projet_RAG — Dépendances Python (backend + évaluation)
#  UTC, LO17/AI31 — Printemps 2026
#
#  Versions épinglées sur un environnement fonctionnel (Python 3.14, Windows).
#  Note : d'anciennes versions plus basses (pydantic 2.7, chromadb 0.5, …) ne
#  compilent pas sur Python 3.14 → on s'en tient aux versions ci-dessous.
#
#  Installation :  pip install -r requirements.txt
# =============================================================================

# ── API (backend FastAPI) ───────────────────────────────────────────
fastapi
uvicorn[standard]==0.48.0
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.2.2

# ── Pipeline RAG (LangChain) ────────────────────────────────────────
langchain==0.3.30
langchain-core==1.4.7
langchain-community==0.3.24
langchain-text-splitters==0.3.11
langchain-mistralai==1.1.5
chromadb==1.5.9

# ── Embeddings & reranker (HuggingFace, CPU) ────────────────────────
# sentence-transformers tire torch + transformers automatiquement.
sentence-transformers==5.5.1

# ── Parsing du corpus RGPD (HTML) ───────────────────────────────────
beautifulsoup4==4.14.3

# ── Évaluation (RAGAS) ──────────────────────────────────────────────
ragas==0.4.3
datasets==5.0.0
pandas==3.0.3
